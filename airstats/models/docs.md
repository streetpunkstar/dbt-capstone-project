{% docs silver_runways_runway_closed %}
Is the runway closed?
Original data sets this to `0` and `1` so assuming `False` is not closed and `True` is closed.
{% enddocs %}


{% docs silver_runways_runway_lighted %}
Is the runway lighted?
Original data sets this to `0` and `1` so assuming `False` is not lighted and `True` is lighted.
{% enddocs %}